# Project State Map

This folder is the canonical landing zone for the E-drive workspace docs.

Start here:

- `CANONICAL_HANDOFF.md`

Then read the source docs in `handoffNew` for the deeper implementation history.

Important reminder:

- `E:\registrarCanon_canon` is the canonical registrar workspace
- older `C:\...` references are stale and should not be treated as current state
